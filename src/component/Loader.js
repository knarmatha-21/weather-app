import { Spinner } from "react-bootstrap";

const Loader = () =>{
    return(
        
            <Spinner animation="border" className="spinnerColor" />
       
    )
}

export default Loader